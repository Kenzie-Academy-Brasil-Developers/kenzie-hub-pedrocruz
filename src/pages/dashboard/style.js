import styled from "styled-components";

export const Main = styled.main`
  flex-direction: row;
  color: white;
  width: 60%;
  margin: 20px auto;

  p {
    font-size: var(--text-size4);
    padding: 20px 0;
  }
`;
